# Chat-outreach machine — state at 2026-09-04

**Status (2026-09-04):** sending STOPPED (queue empty, 11 queueable). Detection running clean
on geo-filtered supply. Verify state with the commands below, never trust these numbers as
current.

```bash
# server-1 address removed for the public repo
ssh <server-1> "sqlite3 /root/flows-orchestrator/ledger.db \"SELECT COUNT(*) FROM domains WHERE delivered_at > strftime('%s','now')-3600\""   # sends/hr
ssh <server-1> "sqlite3 /root/flows-orchestrator/ledger.db 'SELECT detect_status, COUNT(*) FROM domains GROUP BY 1'"
ssh <server-1> "grep -oE 'candidates=[0-9]+ built=[0-9]+' /root/flows-orchestrator/fm_supply.log | tail -5"
```

## Changed in production today (all reversible)

| change | what | undo |
|---|---|---|
| dead-domain re-park | 39,338 rows `error` (were wrongly reset 09-03) | list at `/root/errset_20260903.txt` |
| supply release | 300,000 from `deferred` -> NULL | `/root/unpark_cohort_20260903-110434.txt` |
| **country column** | `domains.country` added, 95.1% populated from StoreLeads `country_code` | column is additive |
| **geo filter** | 246,035 parked `geo_excluded` (detect side) + send side blocked | `UPDATE domains SET detect_status=NULL WHERE detect_status='geo_excluded'` |
| geo add-back | HK TW IL SA QA KW returned (35,529 detect + 1,798 send) | — |
| shopify-agent | 22 marked `shopify_agent_identity_gate`, 1 `shopify_agent_possible_unverified` | non-NULL on purpose so they never re-queue |

Ledger backups on b1: `ledger.db.bak-country-*`, `ledger.db.bak-unreset-*`.

## Allowlist (live)

US CA · GB IE DE FR NL BE LU IT ES PT AT CH SE NO DK FI IS PL CZ SK HU RO BG SI HR EE LV LT GR MT CY · AU NZ SG AE KR JP · HK TW IL SA QA KW

Excluded: IN CN MX PK BR ZA CO CL TR EG PH VN ID RU etc. **51,327 blank-country rows were KEPT.**

## Vendors that actually send (delivered / land rate)

tawk.to 15,473/38.7% · tidio 11,707/28.8% · gorgias 10,511/56.7% · chatra 2,879/**84.4%** ·
crisp 2,327/36.3% · zendesk 1,395/10.4% · zoho-salesiq 939/18.3% · helpscout 617/51.5% ·
zipchat 566/56.9% · olark 440/47.8% · livechat 298/8.3% · reamaze 90/3.8%

hubspot-chat works at 4.0% — deliberately not queued.

## Dead ends proven today — do not re-litigate

- **shopify-agent (48,289 brands): 0 verified deliveries.** Widget is `<shopify-chat>`
  (`AgentShell.show()`, composer `#agent-input` in the `storefront-agent-server.shopify.ai`
  iframe, send `button[aria-label="Send"]`). Send raises a Shop identity gate; its email
  submit (`button.login-form__email-submit`) produces zero auth traffic under automation with
  both `fill()` and real key events. Not a selector problem.
- **Contact form**: adapter exists, blocked by interactive hCaptcha (0/8 measured 2026-06-30,
  proxy and headed both). Not wired into the queue. Unknown: size of the captcha-free subset.
- **`no_widget` is honest** — 1 of 50 had a live widget. No hidden pool.
- **`error` is terminal** — 250-domain paired sample: 402 Shopify "Store unavailable" 30.4%,
  DNS gone 22.0%, 409 parked 18.4%, 404 7.6%. A 60s timeout saved zero.

## Open, not started

1. **Reshard** so the 35,529 added-back brands enter detection (~11h otherwise).
2. **1,467 page-build-blocked brands** (`build_fails=2`). Browser check n=30: **30% alive**
   (9/30) — roughly 440 recoverable, floor not ceiling. Several alive ones are not ecommerce.
   Run the four-way check (`/root/deadproof2.py`) across all 1,467 for a real count.
3. **Encoding fix, tested not shipped**: `scrape_brand.py` `curl()` uses
   `subprocess.run(text=True)` = strict UTF-8; one Windows-1252 byte kills the whole fetch.
   Fix proven on a copy at `/root/fmtest` (rescued 1 of 6). Apply to `/root/flowmap`.
4. **~25,700 written-off brands** (`send_timeout` 86% live, `no_composer` 88% live, vs a 54%
   control) — needs the 3 sender bugs fixed FIRST, then `requeue2.py`. Requeuing before
   fixing reproduces the failure: proven by the 80,988-domain cohort that came back at 5.8%.
5. `requeue.py` is broken (strips 2 files; `pump.sh` scp-overwrites from 7 boxes).
   `requeue2.py` on b1 is the corrected version — compiles, never run.
6. Dashboard `attempted` column reads 3 of 8 files. Delivered column is fine.

## Guardrails added today

`~/.claude/skills/verify-before-claiming/SKILL.md` + `~/.claude/hooks/verify_gate.sh`
(wired into SessionStart alongside docs-guard). Exists because >10 false findings reached
Nikhil on 09-03/04, every one from a single unchecked signal.
